#!/usr/bin/env python3
"""Entry point script for ai-video CLI."""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).parent.parent / "src"))

from ai_video.cli import app

if __name__ == "__main__":
    app()
